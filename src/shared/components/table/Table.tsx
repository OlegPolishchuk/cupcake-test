import React from 'react';
import {TableBody, TableHead} from "components";

export const Table = () => {
  return (
    <table className={'table'}>
      <TableHead />
      <TableBody />
    </table>
  );
};
